{
  "name": "standup-bot-forge",
  "version": "1.0.0",
  "description": "Atlassian Forge app — surfaces standup data natively in Jira",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "forge lint"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/resolver": "^1.6.0",
    "@forge/react": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "~5.6.0",
    "@types/node": "^20.0.0"
  }
}
